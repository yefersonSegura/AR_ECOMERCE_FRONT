export interface ProductModel {
    productID?: number,
    name?: string,
    description?: string,
    price?: number,
    stock?: number,
    categoryID?: number,
    base64Url?: string,
    urlImage?: string,
}